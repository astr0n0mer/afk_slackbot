FROM python:3.14-slim

WORKDIR /afk_slackbot
COPY . /afk_slackbot

RUN apt update && apt install -y git
RUN pip install --upgrade pip
RUN pip install uv
RUN uv pip install --no-cache-dir --system --requirement requirements-dev.txt

CMD ["pytest", "tests", "-vv"]
